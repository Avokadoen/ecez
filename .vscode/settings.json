{
    "cmake.configureOnOpen": false,
    "debug.onTaskErrors": "abort",

    // run zig fmt when saving 
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": ".zig",
                "isAsync": true,
                "cmd": "zig fmt ${file}"
            },
        ]
    }
}